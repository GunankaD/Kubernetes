26. KUBECTL COMMANDS

> doskey k=kubectl $*

1. minikube start --driver=docker

2. kubectl get deployment
3. kubectl get replicaset
4. kubectl get nodes
5. kubectl get pod

CREATE DEPLOYMENTS
this is used to create pods of same type. if we need pods of different type, then we need to create different deployments
6. kubectl create deployment <name-of-deployment> --image=<base-image-for-containers>
ex: kubectl create deployment nginx-depl --image=nginx
ex: kubectl create deployment mongo-depl --image=mongo

this below command creates more replicas of the same pod type
kubectl scale deployment nginx-depl --replicas=3

EDIT DEPLOYMENTS
>choco install vim
>choco install nano
>set KUBE_EDITOR=nano
7. kubectl edit deployment nginx-depl

LOGS AND INFORMATION
8. kubectl logs <pod-id> (which can be found using >kubectl get pods)
9. kubectl describe pod <pod-id>

TO OPEN THE CMD OF ANY CONTAINER
10. kubectl exec -it <pod-id> -- bin/bash

DELETE
11. kubectl delete pods <pod-id>
12. kubectl delete replicaset <replicaset-id>
13. kubectl delete deployment <deployment-name>
- remember, doing 11 or 12 will delete the pod for sure but another would spawn in its place until and unless u delete the deployment as a whole

CREATE DEPLOYMENTS USING CONFIG FILES
14. kubectl apply -f <filename>

EXTRA INFORMATION 
15. kubectl get pods -o wide 
16. kubectl get deployment nginx-depl -o yaml

DELETE DEPLOYMENTS USING CONFIG FILES
17. kubectl delete -f nginx-deployment.yaml

INSTALL INGRESS CONTROLLER IN MINIKUBE (nginx implementation)
18. minikube addons enable ingress
19. k get pods -n ingress-nginx
- the ingress-nginx pods are created in the ingress-nginx namespace 


TO ENABLE DASHBOARD ADDON AND START IT
20. minikube addons enable dashboard
21. minikube dashboard

TO RUN A SINGLE POD
22. k run <pod-name> --iamge=<image-name> --port=<port-no.>

TO RERUN DEPLOYMENTS WHEN NO CHANGES WERE MADE
23. k rollout restart deployment <name>

TO INCREASE THE REPLICA COUNT OF PODS IN A REPLICASET
24. k scale replicaset <rs-name> --replicas=<new-replica-count>

DOCKER NETWORKING
25. to create a bridge docker network
    > docker network create --driver=bridge my-bridge-net
    - other flags include host, overlay, MacLan

26. to list all the networks
    > docker network ls

27. to inspect the network
    > docker network inspect <network-name>PS D:\IMPORTANT\Projects\Kubernetes> docker network inspect my-bridge-net
OUTPUT
[
    {
        "Name": "my-bridge-net",
        "Id": "a491e47da743da83604d3cee15c32dca42d626f7bdbe70fc0f70816a703270bb",
        "Created": "2025-11-25T12:50:54.867260325Z",
        "Scope": "local",
        "Driver": "bridge",
        "EnableIPv4": true,
        "EnableIPv6": false,
        "IPAM": {
            "Driver": "default",
            "Options": {},
            "Config": [
                {
                    "Subnet": "172.18.0.0/16",
                    "Gateway": "172.18.0.1"
                }
            ]
        },
        "Internal": false,
        "Attachable": false,
        "Ingress": false,
        "ConfigFrom": {
            "Network": ""
        },
        "ConfigOnly": false,
        "Containers": {},
        "Options": {},
        "Labels": {}
    }
]


CREATING CONTAINERs USING DOCKER
28. syntax for creating container using docker and not the usual k8s manifest file
    > docker run --name=<container-name> --net=<network-name> <image-name>
29. entering the terminal of a container
    > docker exec -u root -it <container-name> bash
30. deleting or removing container created by docker
    > docker rm <container-name> <container-name>
    > docker stop <container-name> <container-name>
31. restarting a container
    > docker restart <container-name>
